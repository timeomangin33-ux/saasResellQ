export const GoogleTrendsProvider = {
  // Requires installation: `npm i google-trends-api`
  async interestOverTime(keyword: string, timeframe = 'today 12-m') {
    // Placeholder: caller should import 'google-trends-api' and call it directly
    return { keyword, timeframe }
  },
}
